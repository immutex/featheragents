VITE_API_URL=http://localhost:7721

# Optional: only needed for Vite dev mode when not using the ?token=<token>
# URL printed by `feather serve`. In production, the server injects the token;
# in dev you can either set this or open the dashboard via the serve URL.
VITE_API_TOKEN=

VITE_USE_MOCK=false
